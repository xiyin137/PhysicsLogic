#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

bash scripts/assumptions_report.sh ASSUMPTIONS.md
echo "[ok] updated ASSUMPTIONS.md"
